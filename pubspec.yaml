name: another_tus_client
description: A web compatible tus client for dart allowing resumable uploads using the tus protocol.
version: 3.1.4
homepage: https://github.com/olivierb24/another_tus_client

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cross_file: ^0.3.4
  flutter:
    sdk: flutter
  http: ">=1.0.0 <2.0.0"
  speed_test_dart: ^1.0.5+0
  universal_io: ^2.2.2
  web: ^1.1.1

dev_dependencies:
  mockito: ^5.4.4
  build_runner: ^2.4.12
  test: ^1.25.8

platforms:
  android:
  ios:
  web:
  windows:
  macos:
  linux:
